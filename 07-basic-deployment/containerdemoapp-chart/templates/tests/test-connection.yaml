apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "containerdemoapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "containerdemoapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "containerdemoapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
