apiVersion: v1
kind: Service
metadata:
  name: {{ include "second-app-chart.fullname" . }}
  labels:
    {{- include "second-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "second-app-chart.selectorLabels" . | nindent 4 }}
